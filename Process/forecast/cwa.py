# Backward-compatible wrapper — delegates to pagasa_handler (CWA not implemented separately)
# nevermind, I messed up, I created 2 files XD
from .pagasa_handler import make_forecast, make_combined_forecast
